| Suggested addition                                  | Purpose                                                                                                              | Separate story?                        |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| **Validate User Eligibility Before Provisioning**   | Validate user/account/group/environment eligibility before SyncUser                                                  | **Yes**                                |
| **Existing Dataverse User/Reconciliation Handling** | If `systemuser` already exists, don't treat it as a failure; continue security/readiness checks                      | Could be part of SyncUser story        |
| **Poison / Dead-Letter Processing**                 | After retry exhaustion, preserve failed provisioning work for investigation/reprocessing rather than just logging it | **Yes**                                |
| **Concurrency / Duplicate Processing Protection**   | Prevent overlapping timer executions or repeated Graph events from provisioning the same user simultaneously         | Part of end-to-end workflow/resilience |
| **Idempotency**                                     | Reprocessing the same user/change should safely produce the same final state                                         | Part of end-to-end workflow            |
| **429 / Retry-After handling**                      | Respect Graph/Dataverse throttling and bounded concurrency                                                           | Part of retry story                    |
| **Configuration Management**                        | Environment-specific URLs, group IDs, table names, queue names, UAMI client ID, schedule, etc. without hard-coding   | **Yes or pipeline story**              |
| **Baseline/First-Run Behavior**                     | Clearly define what happens when there is no delta token yet                                                         | Part of Graph Delta story              |
| **Poison Message Reprocessing Procedure**           | Support team needs a controlled way to retry failed users                                                            | Runbook/operations                     |
| **End-to-End Never-Logged-In User Test**            | Prove a brand-new user can receive a Dataverse record before first interactive login                                 | **Yes — important**                    |
| **Production Security Validation**                  | Verify Graph, Azure RBAC and Dataverse permissions are actually least privilege                                      | **Yes or release gate**                |
| **Production Readiness / Smoke Test**               | Validate timer, Graph, storage, Dataverse, telemetry and provisioning after deployment                               | Pipeline/release story                 |
